# 优化说明

> 一轮针对**可靠性与响应性**的优化。2026-09-18，共 6 个提交。
> **测试：148 → 186 全过**（+38 条）。改动：+1976 / −54 行，12 个文件。

这一轮不是加功能，而是**把已经在用的东西修到真的可靠**。每一项都来自实际踩到的故障。

---

## 一、总览

| 提交 | 主题 | 解决的问题 |
| --- | --- | --- |
| `41bdc25` | QQ 扫码登录可靠性 | 「反复扫码依然过期」，登录会永久卡死 |
| `e108a11` | 两个工具调用即报错 | `render_card` / `qzone_post` 一调用就炸 |
| `ce188e5` | 派活非阻塞 + 完成通知 | 派一个活，agent 被钉住 20 分钟，IM 静默 |
| `0d18198` | IM 侧裸短语状态入口 | 「是否在线」要排到 agent 后面等几分钟 |
| `80ec020` | 忙时自动回执 | 忙时发消息一片寂静，不知是没收到还是坏了 |
| `4cd80ad` | 长回复自动转 PDF | 「超 300 字发 PDF」只是文档约定，插件不执行 |

---

## 二、逐项说明

### 1. QQ 扫码登录可靠性（`41bdc25`）

**症状**：反复扫码，手机一直提示「二维码已过期」，登录永远不成功。

**根因是三层叠加**：

- **上游**：NapCat 的 `onQRCodeSessionFailed` 只对 `ErrType 1 / ErrCode 3` 自愈。
  其他错误码（真机实测到 `10`、`1`）只打一行日志 —— 不改状态、不换码、
  **连 `loginError` 都不设**。外部看到的永远是「健康等扫码」。
- **上游**：刷新二维码接口返回成功但码没换
  （[NapCatQQ#1962](https://github.com/NapNeko/NapCatQQ/issues/1962)），真机复现过。
- **本项目**：`QR_REUSE_WINDOW_MS` 是 **120 秒**，而二维码寿命**也约 120 秒**。
  用户在第 110 秒说「QQ登录」，扩展把他 110 秒前那张码原样再发一遍。

**修法**：

- 按**二维码年龄**兜底（`refreshStaleQr`），刻意不信 `loginPhase`：
  无人扫 60 秒就换码，已扫码待确认宽限到 115 秒（不打断用户按确认）
- 复用窗口 120s → **60s**，保证投给用户的码至少还剩一半寿命
- 换码时若 NapCat 返回同一张，识别为陈旧并如实提示，不假装成功
- 重新发起登录时**绝不重发**上一轮展示过的码
- 另修一个竞态：`status()` 往返期间登录流程可能已被换掉，作废的循环会把旧码
  再 publish 一遍、盖掉刚换出的新码

### 2. 两个工具一调用就报错（`e108a11`）

- `im_relay_render_card` 的 `execute` 里用了 `h?.config.pdf.browser`，
  但整个作用域**没声明 `h`**（旁边 20 个工具都是 `const h = host()`，只有它漏了）。
  抛出的 `ReferenceError` 又被 `try/catch` 裹成了「渲染卡片失败」，看起来像渲染器坏了。
- `im_relay_qzone_post` 把 `api` 从 channel 上**摘下来裸调用**，`this` 丢了，
  报「Cannot read properties of undefined (reading 'client')」。

**根因**：测试脚本用 `--experimental-strip-types`，**只剥类型不做校验**，
而仓库没有类型检查。这两个都是 `tsc --noEmit` 一眼能抓到的错误。

### 3. 派活非阻塞 + 完成通知（`ce188e5`）

**症状**：派一个任务给外部 agent，调用方就被钉住 —— 实测一次性阻塞 **1210 秒**，
期间用户发来的 IM 消息全在排队。

**修法**（新增 `cbc.mjs`）：

- 默认走后台：**2~5 秒返回一个 id**，调用方立刻恢复自由；前台阻塞改成显式 `--fg`
- `--status` 查进度、`--result` 取结论
- `--notify` 挂**脱离进程的守护**：轮询到完成后**直接推 QQ 通知**，不经过 agent、零 token

### 4. IM 侧裸短语状态入口（`0d18198`）

**症状**：用户在 IM 里连发三次状态询问，三次全在排队。

**根因**：IM 消息只有 `/` 开头的才走命令快通道（`parseCommand` 要求 `startsWith("/")`），
**裸短语一律排队等 agent**。

> 注意别和 `triggers.ts` 搞混：那个 `matchLocalTrigger` 只挂在 pi 的**本地 input 事件**上，
> 而且开头的 `event.source === "extension"` 就把 IM 注入的消息排除了；
> 它命中后也只是 `ctx.ui.notify` **弹本地通知**，根本不是 IM 回复。

**修法**：新增 `matchImQuickQuery()`：

- **状态类**（`状态` / `im状态` / `机器人状态` / `是否在线` / `在线吗` / `活着吗` …）
  → **永远就地秒回**，纯机械答案，零 token
- **进展类**（`进度` / `在干什么` / `忙完了吗` …）→ **只在 agent 忙时拦截**；
  空闲时放行给 agent（那种问题该由它去查真实进展）

### 5. 忙时自动回执（`80ec020`）

上一项只解决了「状态问句」，**普通消息**（比如"帮我改个 X"）在忙时仍然静默排队。

**修法**：消息入队后，若「这条真的会等」就立刻回一句：
正在做谁的那条、已跑多久、前面还有几条、怎么打断。

**两个门防止它变成噪音**：

1. **只在真会等的时候回** —— 空闲时 `pump()` 立刻就把消息送给 pi 了，
   再回一句「收到我在忙」既是噪音又是误导
2. **同一会话 60 秒内只回一次** —— 连发五条追问不该收到五条回执

### 6. 长回复自动转 PDF（`4cd80ad`）

**症状**：「结论超过 300 字或含表格 → 发 PDF」这条约定写在 `AGENT.md` 里，
但那一直**只是给模型看的提醒**，模型忘了就退化成手机上刷屏的长文。

**更关键的是**：`pdf.ts` 里 `shouldUsePdf()` **早就写好了，全项目却只在测试里被调用过** ——
生产链路一次都没接上。

**修法**：`onSettled` 改走 `deliverFinal()`：超阈值 → 渲染 PDF → 发文件 →
聊天里只留一句「📄 这条内容较长（N 字），已渲染成 PDF 发给你：<标题>（大小）」。

**两道安全网**（任何情况下都不能把消息弄丢）：

1. 通道没有 `sendFile`（微信侧只做出站文本）→ 直接发原文
2. 渲染或发送任一步失败 → 退回发原文

---

## 三、诊断中挖出来的三个「文档里没写」的事实

记下来，省得下次再查一遍。

1. **`triggers.ts` 只作用于本机对话框，不作用于 IM。**
   它挂在 `pi.on("input")` 且显式排除 `source === "extension"`（即 IM 注入）。
   IM 侧唯一的快通道是 `/` 开头的命令。

2. **`shouldUsePdf()` 从没被生产代码调用过。**
   函数在、测试在、配置项在（`pdf.enabled` / `pdf.threshold`），就是没人接。

3. **NapCat 只对一种错误码自愈。**
   `onQRCodeSessionFailed` 里只有 `u === 1 && l === 3` 会触发重出码，
   其他错误码它静默 —— 这是「登录永久卡死」的机制。

---

## 四、开发时踩过的坑（给后来者）

同一类病：**出错了，但没人看得见**。

| 坑 | 现象 | 教训 |
| --- | --- | --- |
| `git show <rev>^:file` 在 Windows | node 的 `execSync` 走 `cmd.exe`，`^` 是转义符，被吃掉后比的是**同一个版本** | 取父提交用 `git rev-parse <rev>^` 拿 hash，别让 shell 有机会吃字符 |
| `CODEBUDDY_FORCE_HEADLESS_BUNDLE=1` | 让 `agents` 命令**静默失效**（退出码仍为 0，stdout 为空） | 只给真正需要 headless 的调用加这个 env |
| `import.meta.url` 的 `.pathname` | 是 **URL 编码**的（空格变 `%20`），拼出的路径不存在 | 先 `decodeURIComponent`；另外 `detached + stdio:'ignore'` 会吞掉启动失败，要留日志 |
| `listJobs()` 解析失败 `return []` | 把失败吞成了「空列表」，导致状态永远误报 `running` | 解析失败要**抛错并带上 stderr**，不要静默降级 |

---

## 五、已知限制

- **扩展的代码改动不会热加载**（只有配置会），改完要**重启 pi** 才生效。
- **微信通道不支持发文件** → 长回复自动转 PDF 在微信侧会退化成发原文
  （这是刻意的安全网，不是 bug）。
- **`progress` 默认 `off`**（代码默认值是 `live`）。
  打开后 agent 每执行一个工具会推一条 `🔧 摘要`（节流 5 秒）给 IM。
